/**
 * 注意：JS和TS中类型单词都是小写的
 */

let age: number = 18;

let myName: string = '小余';

let isLoading: boolean = false;

let aNull: null = null;

let bUndefined: undefined = undefined;

let sSymbol: symbol = Symbol();
