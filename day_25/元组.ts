/**
 * 《元组》
 * 解释：
 * 1. 元组类型可以确切的标记出有多少个元素，以及每个元素的类型。
 * 使用场景：
 * 1. 当确切的知道数组中包含多少个元素，以及每个元素的类型时。
 */

// 默认写法
let postion: number[] = [ 1, 2, 3, 4, 5, 6, 7, 8 ];

// 元组写法
let postion2: [number, string] = [ 23, 'xiaoyu' ];
